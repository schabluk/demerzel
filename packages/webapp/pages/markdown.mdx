import { withWidth } from '@material-ui/core'

import Container from '../layout/Container'

export default ({ children, screenSize }) => {
  return (
    <Container style={{ flex: '1' }}>{children}</Container>
  )
}

### Markdown

<div style={{ color: 'red' }}>HTML syntax support</div>

_italic_

**bold**
